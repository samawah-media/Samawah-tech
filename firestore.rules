rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAdmin() {
      return request.auth != null
        && request.auth.token.email == "omarsamawah@gmail.com";
    }

    match /projects/{document=**} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /showcase_links/{document=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
  }
}
